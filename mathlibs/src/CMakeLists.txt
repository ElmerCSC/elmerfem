
IF(NOT BLAS_FOUND)
  ADD_SUBDIRECTORY(blas)
ENDIF()
IF(NOT LAPACK_FOUND)
  ADD_SUBDIRECTORY(lapack)
ENDIF()

IF(NOT EXTERNAL_ARPACK)
  ADD_SUBDIRECTORY(arpack)
ENDIF()

IF(MPI_FOUND AND NOT EXTERNAL_PARPACK)
    ADD_SUBDIRECTORY(parpack)
ENDIF()
