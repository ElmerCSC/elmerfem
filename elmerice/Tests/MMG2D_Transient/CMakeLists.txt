IF (HAVE_MMG)
 INCLUDE(${CMAKE_CURRENT_SOURCE_DIR}/../test_macros.cmake)

 FILE(COPY PROG MESH_I0_J0 SIFs DESTINATION "${CMAKE_CURRENT_BINARY_DIR}/")
 ADD_ELMERICETEST_MODULE(MMG2D_Transient USFs ./PROG/USFs.F90)

 ADD_ELMERICE_TEST(MMG2D_Transient)
 ADD_ELMERICE_LABEL(MMG2D_Transient mmg)

ENDIF()
