INCLUDE(${CMAKE_CURRENT_SOURCE_DIR}/../test_macros.cmake)

CONFIGURE_FILE(flowheight.sif flowheight.sif COPYONLY)

FILE(COPY ELMERSOLVER_STARTINFO mesh.grd PROG DESTINATION "${CMAKE_CURRENT_BINARY_DIR}/")

ADD_ELMERICE_TEST(FlowHeight)
