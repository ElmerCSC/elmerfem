INCLUDE(${CMAKE_CURRENT_SOURCE_DIR}/../test_macros.cmake)

CONFIGURE_FILE(sia.sif sia.sif COPYONLY)

FILE(COPY ELMERSOLVER_STARTINFO mesh_A.grd DESTINATION "${CMAKE_CURRENT_BINARY_DIR}/")

ADD_ELMERICE_TEST(SIA)
