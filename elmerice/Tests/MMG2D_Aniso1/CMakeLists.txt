IF (HAVE_MMG)
 INCLUDE(${CMAKE_CURRENT_SOURCE_DIR}/../test_macros.cmake)

 CONFIGURE_FILE(Case_Aniso.sif Case_Aniso.sif COPYONLY)
 FILE(COPY ELMERSOLVER_STARTINFO square.msh DESTINATION "${CMAKE_CURRENT_BINARY_DIR}/")

 ADD_ELMERICE_TEST(MMG2D_Aniso1)
 ADD_ELMERICE_LABEL(MMG2D_Aniso1 mmg)
 ADD_ELMERICE_LABEL(MMG2D_Aniso1 elmerice-fast)

ENDIF()
