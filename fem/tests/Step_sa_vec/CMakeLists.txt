INCLUDE(test_macros)
INCLUDE_DIRECTORIES(${CMAKE_BINARY_DIR}/fem/src)

CONFIGURE_FILE( Step_sa_vec.sif Step_sa_vec.sif COPYONLY)

file(COPY ELMERSOLVER_STARTINFO Step.grd Step DESTINATION "${CMAKE_CURRENT_BINARY_DIR}/")

ADD_ELMER_TEST(Step_sa_vec LABELS restart)
