IF(HAVE_AMGX)
INCLUDE(test_macros)
INCLUDE_DIRECTORIES(${CMAKE_BINARY_DIR}/fem/src)
CONFIGURE_FILE(case.sif case.sif COPYONLY)
file(COPY ELMERSOLVER_STARTINFO winkel.grd linsys.sif config.json DESTINATION "${CMAKE_CURRENT_BINARY_DIR}/")
ADD_ELMER_TEST(WinkelBmPoissonAMGX NPROCS 1 2 4 LABELS amgx)
ENDIF()
