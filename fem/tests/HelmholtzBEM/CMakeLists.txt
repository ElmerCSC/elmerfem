INCLUDE(test_macros)
INCLUDE_DIRECTORIES(${CMAKE_BINARY_DIR}/fem/src)

CONFIGURE_FILE( HelmholtzBEM.sif HelmholtzBEM.sif COPYONLY)

file(COPY ELMERSOLVER_STARTINFO CircleInBox.mif MeshFiles DESTINATION "${CMAKE_CURRENT_BINARY_DIR}/")

ADD_ELMER_TEST(HelmholtzBEM)
ADD_ELMER_LABEL(HelmholtzBEM quick)
