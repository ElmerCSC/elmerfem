INCLUDE(test_macros)
INCLUDE_DIRECTORIES(${CMAKE_BINARY_DIR}/fem/src)

CONFIGURE_FILE( Step_v2f.sif Step_v2f.sif COPYONLY)

file(COPY ELMERSOLVER_STARTINFO Step_v2f.grd DESTINATION "${CMAKE_CURRENT_BINARY_DIR}/")

ADD_ELMER_TEST(Step_v2f)
