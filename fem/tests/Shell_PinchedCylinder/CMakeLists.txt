INCLUDE(test_macros)
INCLUDE_DIRECTORIES(${CMAKE_BINARY_DIR}/fem/src)

CONFIGURE_FILE( pointload.sif pointload.sif COPYONLY)

file(COPY ELMERSOLVER_STARTINFO Mesh DESTINATION "${CMAKE_CURRENT_BINARY_DIR}/")

ADD_ELMER_TEST(Shell_PinchedCylinder LABELS shell)

