INCLUDE(test_macros)
INCLUDE_DIRECTORIES(${CMAKE_BINARY_DIR}/fem/src)

CONFIGURE_FILE( Q3.sif Q3.sif COPYONLY)

file(COPY ELMERSOLVER_STARTINFO Q3Mesh DESTINATION "${CMAKE_CURRENT_BINARY_DIR}/")

ADD_ELMER_TEST(Shell_OpenHemisphere_Third_Order_Nodal LABELS shell)
