INCLUDE(test_macros)
INCLUDE_DIRECTORIES(${CMAKE_BINARY_DIR}/fem/src)

CONFIGURE_FILE(case.sif case.sif COPYONLY)
ADD_ELMERTEST_MODULE(HeatUnisoUDF HeatCondUniso HeatCondUniso.F90)

file(COPY ELMERSOLVER_STARTINFO HeatCondUniso.F90 cross.grd DESTINATION "${CMAKE_CURRENT_BINARY_DIR}/")

ADD_ELMER_TEST(HeatUnisoUDF)
