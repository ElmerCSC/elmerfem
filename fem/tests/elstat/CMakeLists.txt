INCLUDE(test_macros)
INCLUDE_DIRECTORIES(${CMAKE_BINARY_DIR}/fem/src)

CONFIGURE_FILE( elstatics.sif elstatics.sif COPYONLY)

file(COPY ELMERSOLVER_STARTINFO elmesh.grd DESTINATION "${CMAKE_CURRENT_BINARY_DIR}/")

ADD_ELMER_TEST(elstat)
