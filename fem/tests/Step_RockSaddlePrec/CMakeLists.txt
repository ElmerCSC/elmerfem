IF(HAVE_ROCALUTION)
INCLUDE(test_macros)
INCLUDE_DIRECTORIES(${CMAKE_BINARY_DIR}/fem/src)

CONFIGURE_FILE( Step.sif Step.sif COPYONLY)

file(COPY ELMERSOLVER_STARTINFO Step.grd StepRes.gif StepMesh.gif  DESTINATION "${CMAKE_CURRENT_BINARY_DIR}/")

ADD_ELMER_TEST(Step_RockSaddlePrec LABELS quick rocalution)
ENDIF(HAVE_ROCALUTION)
