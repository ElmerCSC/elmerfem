IF(HAVE_UMFPACK)
  INCLUDE(test_macros)
  INCLUDE_DIRECTORIES(${CMAKE_BINARY_DIR}/fem/src)

  CONFIGURE_FILE(case.sif case.sif COPYONLY)

  file(COPY ELMERSOLVER_STARTINFO comb.grd linsys.sif DESTINATION "${CMAKE_CURRENT_BINARY_DIR}/")

  ADD_ELMER_TEST(CombPoissonInternalFETI NPROCS 2 LABELS feti)
ENDIF()
