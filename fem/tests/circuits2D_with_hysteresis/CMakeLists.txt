INCLUDE(test_macros)
INCLUDE_DIRECTORIES(${CMAKE_BINARY_DIR}/fem/src)
CONFIGURE_FILE(ELMERSOLVER_STARTINFO ELMERSOLVER_STARTINFO COPYONLY)
file(COPY sif DESTINATION "${CMAKE_CURRENT_BINARY_DIR}/")
file(COPY 37 DESTINATION "${CMAKE_CURRENT_BINARY_DIR}/")
ADD_ELMER_TEST(circuits2D_with_hysteresis LABELS circuits 2D mgdyn)
