INCLUDE(test_macros)
INCLUDE_DIRECTORIES(${CMAKE_BINARY_DIR}/fem/src)

CONFIGURE_FILE( ad4.sif ad4.sif COPYONLY)

file(COPY ELMERSOLVER_STARTINFO  Step2.grd DESTINATION "${CMAKE_CURRENT_BINARY_DIR}/")

ADD_ELMER_TEST(adv_diff4)
