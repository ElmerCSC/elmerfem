IF(HAVE_PARMMG)
IF(HAVE_ZOLTAN)
IF(ParMetis_FOUND)
  INCLUDE(test_macros)
  INCLUDE_DIRECTORIES(${CMAKE_BINARY_DIR}/fem/src)

  CONFIGURE_FILE(parallelremesh.sif parallelremesh.sif COPYONLY)

  ADD_ELMERTEST_MODULE(ParallelRemeshParmetis TestLevelSet ./TestLevelSet.F90)

  FILE(COPY ELMERSOLVER_STARTINFO cube.msh DESTINATION "${CMAKE_CURRENT_BINARY_DIR}/")

  FILE(MAKE_DIRECTORY "${CMAKE_CURRENT_BINARY_DIR}/Results/cube")

  ADD_ELMER_TEST(ParallelRemeshParmetis NPROCS 4 LABELS mmg slow)
ENDIF()
ENDIF()
ENDIF()
