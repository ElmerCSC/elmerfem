INCLUDE(test_macros)
INCLUDE_DIRECTORIES(${CMAKE_BINARY_DIR}/fem/src)

CONFIGURE_FILE( Free.sif Free.sif COPYONLY)

file(COPY ELMERSOLVER_STARTINFO MESHDIR  DESTINATION "${CMAKE_CURRENT_BINARY_DIR}/")

ADD_ELMER_TEST(freesurf_int)
ADD_ELMER_LABEL(freesurf_int quick)
