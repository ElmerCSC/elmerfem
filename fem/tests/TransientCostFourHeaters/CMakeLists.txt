INCLUDE(test_macros)
INCLUDE_DIRECTORIES(${CMAKE_BINARY_DIR}/fem/src)

CONFIGURE_FILE(case.sif case.sif COPYONLY)
file(COPY ELMERSOLVER_STARTINFO spots.grd t1.dat t2.dat t3.dat t4.dat DESTINATION "${CMAKE_CURRENT_BINARY_DIR}/")

ADD_ELMER_TEST(TransientCostFourHeaters)
