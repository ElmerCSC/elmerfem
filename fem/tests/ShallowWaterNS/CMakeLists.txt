INCLUDE(test_macros)
INCLUDE_DIRECTORIES(${CMAKE_BINARY_DIR}/fem/src)

CONFIGURE_FILE( ptest.sif ptest.sif COPYONLY)

file(COPY ELMERSOLVER_STARTINFO 1d.grd DESTINATION "${CMAKE_CURRENT_BINARY_DIR}/")

ADD_ELMER_TEST(ShallowWaterNS)
