INCLUDE(test_macros)
INCLUDE_DIRECTORIES(${CMAKE_BINARY_DIR}/fem/src)

CONFIGURE_FILE(case3d.sif case3d.sif COPYONLY)

file(COPY OnePipe.msh ELMERSOLVER_STARTINFO DESTINATION "${CMAKE_CURRENT_BINARY_DIR}/")

ADD_ELMER_TEST(ThermalPipe3D NPROCS 1 LABELS heateq matc)
