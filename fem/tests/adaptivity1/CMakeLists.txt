
INCLUDE(${CMAKE_CURRENT_SOURCE_DIR}/../test_macros.cmake)

INCLUDE_DIRECTORIES(${CMAKE_BINARY_DIR}/fem/src)

CONFIGURE_FILE(ELMERSOLVER_STARTINFO ELMERSOLVER_STARTINFO COPYONLY)
CONFIGURE_FILE(Ldomain.sif Ldomain.sif COPYONLY)
CONFIGURE_FILE(Ldomain.mif Ldomain.mif COPYONLY)

# Create directory for mesh
FILE(MAKE_DIRECTORY ${CMAKE_CURRENT_BINARY_DIR}/Ldomain)

ADD_ELMER_TEST(adaptivity1)