INCLUDE(test_macros)
INCLUDE_DIRECTORIES(${CMAKE_BINARY_DIR}/fem/src)

CONFIGURE_FILE(case_b.sif case_b.sif COPYONLY)

file(COPY ELMERSOLVER_STARTINFO beam_b.grd case_a.msh DESTINATION "${CMAKE_CURRENT_BINARY_DIR}/")

ADD_ELMER_TEST(Outlet2InletBCv2 LABELS quick)
