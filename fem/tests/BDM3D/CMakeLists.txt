INCLUDE(test_macros)
INCLUDE_DIRECTORIES(${CMAKE_BINARY_DIR}/fem/src)

CONFIGURE_FILE( BDM.sif BDM.sif COPYONLY)
ADD_ELMERTEST_MODULE(BDM3D BDMSolve BDMSolve.f90)

file(COPY ELMERSOLVER_STARTINFO cube BDMSolve.f90 DESTINATION "${CMAKE_CURRENT_BINARY_DIR}/")

ADD_ELMER_TEST(BDM3D)
