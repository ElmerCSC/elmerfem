INCLUDE(test_macros)
INCLUDE_DIRECTORIES(${CMAKE_BINARY_DIR}/fem/src)

CONFIGURE_FILE( Pipe.sif Pipe.sif COPYONLY)

file(COPY ELMERSOLVER_STARTINFO Pipe.grd  DESTINATION "${CMAKE_CURRENT_BINARY_DIR}/")

ADD_ELMER_TEST(PorousPipe)
ADD_ELMER_LABEL(PorousPipe quick)
