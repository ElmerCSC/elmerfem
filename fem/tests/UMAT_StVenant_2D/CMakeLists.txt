INCLUDE(test_macros)
INCLUDE_DIRECTORIES(${CMAKE_BINARY_DIR}/fem/src)

CONFIGURE_FILE( elasticity.sif elasticity.sif COPYONLY)
ADD_ELMERTEST_MODULE(UMAT_StVenant_2D MyUMAT MyUMAT.F90)

file(COPY beam.grd ELMERSOLVER_STARTINFO MyUMAT.F90 DESTINATION "${CMAKE_CURRENT_BINARY_DIR}/")

ADD_ELMER_TEST(UMAT_StVenant_2D LABELS elasticity umat quick)
