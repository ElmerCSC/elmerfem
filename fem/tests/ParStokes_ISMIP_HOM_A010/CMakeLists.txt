INCLUDE(test_macros)
INCLUDE_DIRECTORIES(${CMAKE_BINARY_DIR}/fem/src)

CONFIGURE_FILE( parstokes.sif parstokes.sif COPYONLY)

file(COPY ELMERSOLVER_STARTINFO Mesh DESTINATION "${CMAKE_CURRENT_BINARY_DIR}/")

ADD_ELMER_TEST(ParStokes_ISMIP_HOM_A010 LABELS elmerice slow)

