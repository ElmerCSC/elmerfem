IF(HAVE_ZOLTAN)
INCLUDE(test_macros)
INCLUDE_DIRECTORIES(${CMAKE_BINARY_DIR}/fem/src)

CONFIGURE_FILE( AdvReactDG.sif AdvReactDG.sif COPYONLY)

file(COPY square.grd ELMERSOLVER_STARTINFO  DESTINATION "${CMAKE_CURRENT_BINARY_DIR}/")

ADD_ELMER_TEST(AdvReactDGZoltan NPROCS 6 LABELS zoltan)
ENDIF()
