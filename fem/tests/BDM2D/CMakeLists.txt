INCLUDE(test_macros)
INCLUDE_DIRECTORIES(${CMAKE_BINARY_DIR}/fem/src)

CONFIGURE_FILE( BDM.sif BDM.sif COPYONLY)
ADD_ELMERTEST_MODULE(BDM2D BDMSolve BDMSolve.f90)

file(COPY ELMERSOLVER_STARTINFO square BDMSolve.f90 DESTINATION "${CMAKE_CURRENT_BINARY_DIR}/")

ADD_ELMER_TEST(BDM2D LABELS quick vector_element)
