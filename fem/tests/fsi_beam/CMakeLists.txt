INCLUDE(${CMAKE_CURRENT_SOURCE_DIR}/../test_macros.cmake)
INCLUDE_DIRECTORIES(${CMAKE_BINARY_DIR}/fem/src)

CONFIGURE_FILE( fsi.sif fsi.sif COPYONLY)
ADD_ELMERTEST_MODULE(fsi_beam FsiStuff FsiStuff.f90)

file(COPY ELMERSOLVER_STARTINFO fsi.grd TEST.RESULT FsiStuff.f90 DESTINATION "${CMAKE_CURRENT_BINARY_DIR}/")

ADD_ELMER_TEST(fsi_beam)
