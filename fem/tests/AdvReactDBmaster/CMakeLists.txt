IF(HAVE_MUMPS)
INCLUDE(test_macros)
INCLUDE_DIRECTORIES(${CMAKE_BINARY_DIR}/fem/src)

CONFIGURE_FILE( AdvReactDB.sif AdvReactDB.sif COPYONLY)

file(COPY square.grd ELMERSOLVER_STARTINFO  DESTINATION "${CMAKE_CURRENT_BINARY_DIR}/")

ADD_ELMER_TEST(AdvReactDBmaster LABELS quick)
ENDIF()
