$ Circuits = 3



! ------------------------ Circuit 1 ------------------------
! Define variable count and initialize circuit matrices.
$ C.1.perm = zeros(4)
$ C.1.perm(0) = 0
$ C.1.perm(2) = 1
$ C.1.perm(3) = 2
$ C.1.perm(1) = 3

$ C.1.variables = 4
$ C.1.A = zeros(4,4)
$ C.1.B = zeros(4,4)
$ C.1.Mre = zeros(4,4)
$ C.1.Mim = zeros(4,4)

! Define variables.
$ C.1.name.1 = "i_S_1"
$ C.1.name.2 = "v_S_1"
$ C.1.name.3 = "i_component(1)"
$ C.1.name.4 = "v_component(1)"

! Define sources:
!------------------
$ C.1.B(0,0) = 1
$ C.1.source.1.re = "S1 Re"
$ C.1.source.1.im = "S1 Im"

! Define network cycles:
!-------------------------
!Voltage relations.
$ C.1.B(1,3) = 1
$ C.1.B(1,1) = -1
!Current relations.
$ C.1.B(2,2) = 1
$ C.1.B(2,0) = 1


! ------------------------ Circuit 2 ------------------------
! Define variable count and initialize circuit matrices.
$ C.2.perm = zeros(4)
$ C.2.perm(0) = 0
$ C.2.perm(2) = 1
$ C.2.perm(3) = 2
$ C.2.perm(1) = 3

$ C.2.variables = 4
$ C.2.A = zeros(4,4)
$ C.2.B = zeros(4,4)
$ C.2.Mre = zeros(4,4)
$ C.2.Mim = zeros(4,4)

! Define variables.
$ C.2.name.1 = "i_S_2"
$ C.2.name.2 = "v_S_2"
$ C.2.name.3 = "i_component(2)"
$ C.2.name.4 = "v_component(2)"

! Define sources:
!------------------
$ C.2.B(0,0) = 1
$ C.2.source.1.re = "S2 Re"
$ C.2.source.1.im = "S2 Im"

! Define network cycles:
!-------------------------
!Voltage relations.
$ C.2.B(1,3) = 1
$ C.2.B(1,1) = -1
!Current relations.
$ C.2.B(2,2) = 1
$ C.2.B(2,0) = 1


! ------------------------ Circuit 3 ------------------------
! Define variable count and initialize circuit matrices.
$ C.3.perm = zeros(4)
$ C.3.perm(0) = 0
$ C.3.perm(2) = 1
$ C.3.perm(3) = 2
$ C.3.perm(1) = 3

$ C.3.variables = 4
$ C.3.A = zeros(4,4)
$ C.3.B = zeros(4,4)
$ C.3.Mre = zeros(4,4)
$ C.3.Mim = zeros(4,4)

! Define variables.
$ C.3.name.1 = "i_S_3"
$ C.3.name.2 = "v_S_3"
$ C.3.name.3 = "i_component(3)"
$ C.3.name.4 = "v_component(3)"

! Define sources:
!------------------
$ C.3.B(0,0) = 1
$ C.3.source.1.re = "S3 Re"
$ C.3.source.1.im = "S3 Im"

! Define network cycles:
!-------------------------
!Voltage relations.
$ C.3.B(1,3) = 1
$ C.3.B(1,1) = -1
!Current relations.
$ C.3.B(2,2) = 1
$ C.3.B(2,0) = 1
