INCLUDE(test_macros)
INCLUDE_DIRECTORIES(${CMAKE_BINARY_DIR}/fem/src)

CONFIGURE_FILE( PoissonBEM.sif PoissonBEM.sif COPYONLY)

file(COPY ELMERSOLVER_STARTINFO  heater.grd DESTINATION "${CMAKE_CURRENT_BINARY_DIR}/")

ADD_ELMER_TEST(PoissonBEM)
ADD_ELMER_LABEL(PoissonBEM quick)
