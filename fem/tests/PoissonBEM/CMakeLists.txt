INCLUDE(${CMAKE_CURRENT_SOURCE_DIR}/../test_macros.cmake)
INCLUDE_DIRECTORIES(${CMAKE_BINARY_DIR}/fem/src)

CONFIGURE_FILE( PoissonBEM.sif PoissonBEM.sif COPYONLY)

file(COPY ELMERSOLVER_STARTINFO TEST.RESULT heater.grd DESTINATION "${CMAKE_CURRENT_BINARY_DIR}/")

ADD_ELMER_TEST(PoissonBEM)
