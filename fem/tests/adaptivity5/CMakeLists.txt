INCLUDE(test_macros)
INCLUDE_DIRECTORIES(${CMAKE_BINARY_DIR}/fem/src)

CONFIGURE_FILE( stress.sif stress.sif COPYONLY)

file(COPY ELMERSOLVER_STARTINFO mesh.grd  DESTINATION "${CMAKE_CURRENT_BINARY_DIR}/")

ADD_ELMER_TEST(adaptivity5)
ADD_ELMER_LABEL(adaptivity5 quick)
